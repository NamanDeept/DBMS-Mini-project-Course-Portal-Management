
create table Student(Enrollment varchar(20) PRIMARY KEY,Name varchar(20),Pass varchar(20));

create table Faculty(FacID varchar(20) PRIMARY KEY,Name varchar(20),Pass varchar(20));
create table Courses(CourseCode varchar(10) PRIMARY KEY,Name varchar(30),Description varchar(255));

/*Blob datatype is used for storing the file*/
